services:
  skima:
    build: .
    container_name: skima-demo
    ports:
      - "3000:3001"
    environment:
      - DEMO_MODE=true
    volumes:
      - skima-data:/app/data
    restart: unless-stopped

volumes:
  skima-data:
